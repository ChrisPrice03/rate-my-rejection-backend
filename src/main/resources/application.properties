spring.application.name=rate-my-rejection

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/mydb
spring.datasource.username=root
spring.datasource.password=IUSucks2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
#spring.jpa.hibernate.ddl-auto=update  # Automatically update database schema (use 'none' or 'validate' in production)
#spring.jpa.properties.hibernate.format_sql=true  # Format SQL queries for readability